$(function () {

	$('.audio-slider').slick({
		fade: true,
		// slidesToShow: 1,
		// slidesToScroll: 1,
		// // asNavFor: '.slider-for',
		// dots: true,
		// // centerMode: true,
		// // focusOnSelect: true,
		// variableWidth: true
	});

	new WOW().init();

});
